class Test {
	void main(String[] args) {}
}

class ClassDec extends Parent {
	int attribute;

	int mathod(int a, Id b) {
		// StmtWithoutId
		while(a) {
			System.out.println("Hello, World!");
			continue;
			if(a == 1)
				break;
		}

		if(b == 1)
			id = b;

		if(b > a)
			/* Semicolon Stmt */;
		else
			return a;

		// AfterThisInStmt
		this.id.id()[3];
		this.id();
		this.id()[3] = 2;
		this.id = 2;
		this.id[][] id;

		// AfterId
		Id id = 2;
		Id[] id = 2;

		// NonClassVarDec
		boolean id = true;
		int[] id = {2,2};

	}

	void noParams() {
		return;
	}
}

class Expression {
	void expressions() {
		// Tp, X, Xp, P, T2, T3
		a = "oi".length();
		a = "oi".length;
		a = "oi".substring(1);
		a = "oi".substring(1,2);
		a = "oi".id();
		a = "oi".id(1, 2);
		a = id[2];

		// TT
		a = 1;
		b = true;
		b = false;
		a = b;
		x = "oi";
		x = null;
		x = new int[2];
		x = new boolean[2];
		x = new Car[2];
		x = new Car();

		// AfterThisInExp, RestThisInExp
		a = this;
		a = this.id;
		a = this.id();

		// R, El
		b = (23);
		a = {1,2};
		b = {};

		// RelOp
		b = a < 2;
		b = a <= 2;
		b = a >= 2;
		b = a > 2;
		b = a == 2;
		b = a != 2;

		// BoolOp
		b = a && b;
		b = a || b;

		// AddOp
		b = a+b-c;

		// MultOp
		b = a*b/c%d;

		// Unop
		b = -a+b;
		b = !b;
	}
}